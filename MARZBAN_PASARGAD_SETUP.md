# راهنمای اتصال پنل‌های VPN (مرزبان و پاسارگارد) — قالب فروشی

این ربات به صورت پیش‌فرض یک قالب خام و آماده فروش است. تمام اطلاعات شخصی و
کسب‌وکاری از آن حذف شده و باید توسط خریدار/ادمین از طریق فایل `.env` یا از
داخل پنل ادمین (بخش «اطلاعات ربات») تنظیم شود.

## پنل‌های VPN پشتیبانی‌شده

ربات از **دو پنل VPN** پشتیبانی می‌کند که هر دو از نظر عملکرد مشابه هم‌اند
(ساخت/تمدید/فعال‌سازی/غیرفعال‌سازی خودکار سرویس از طریق REST API پنل):

1. **مرزبان (Marzban)** — https://github.com/Gozargah/Marzban
2. **پاسارگارد (PasarGuard)** — https://github.com/PasarGuard/panel (یک پنل VPN
   مستقل، **نه یک درگاه پرداخت**؛ از نظر ساختار شبیه مرزبان است)

می‌توانید فقط یکی از این دو پنل را وصل کنید، یا هر دو را همزمان وصل کنید و از
پنل ادمین ربات مشخص کنید کدام‌یک «پنل فعال» باشد (یعنی ساخت/تمدید/فعال‌سازی
سرویس‌های واقعی مشتریان از طریق کدام پنل انجام شود). درگاه پرداخت آنلاین ربات
(یونیک‌پی) کاملاً مستقل از این انتخاب است.

## ۱) اتصال به مرزبان (Marzban)

متغیرهای محیطی لازم در `.env`:

```
MARZBAN_BASE_URL=https://your-marzban-panel.example.com
MARZBAN_USERNAME=admin
MARZBAN_PASSWORD=your-password
```

پس از تنظیم، از داخل پنل ادمین ربات، بخش «🔗 اتصال پنل مرزبان» را باز کنید و
روی «تست اتصال» بزنید. اگر متصل شد، می‌توانید قالب‌های (Templates) ساخته‌شده
در مرزبان را به پلن‌های VIP/Gaming ربات نگاشت (map) کنید.

## ۲) اتصال به پاسارگارد (PasarGuard)

⚠️ پاسارگارد یک **پنل مدیریت VPN** است، درست مثل مرزبان — یک درگاه پرداخت
آنلاین نیست.

متغیرهای محیطی لازم در `.env`:

```
PASARGAD_BASE_URL=https://your-pasarguard-panel.example.com
PASARGAD_USERNAME=admin
PASARGAD_PASSWORD=your-password
```

پس از تنظیم، از داخل پنل ادمین ربات، بخش «🛡️ اتصال پنل پاسارگارد» را باز کنید
و روی «تست اتصال» بزنید.

⚠️ توجه: پاسارگارد (PasarGuard) به‌سرعت در حال توسعه است و مسیرهای API آن
ممکن است بین نسخه‌های مختلف کمی تفاوت داشته باشد. اگر «تست اتصال» با خطای
404/۴۰۴ مواجه شد، مسیرهای Swagger پنل خودتان را از آدرس `/docs` (در صورت فعال
بودن پرچم `DOCS` در تنظیمات پنل) بررسی کنید و در صورت نیاز مسیرهای داخل فایل
`pasargad.py` را با آن‌ها مطابقت دهید.

## ۳) انتخاب پنل فعال (وقتی هر دو وصل باشند)

اگر هم مرزبان و هم پاسارگارد را وصل کرده باشید، از پنل ادمین ربات بخش «🔀
انتخاب پنل VPN فعال» را باز کنید و یکی از دو پنل را به‌عنوان «پنل فعال»
انتخاب کنید. تمام عملیات ساخت/تمدید/فعال‌سازی/غیرفعال‌سازی سرویس مشتریان از
آن پس فقط از طریق همان پنل انتخاب‌شده انجام می‌شود؛ در هر لحظه می‌توانید
پنل فعال را از همین بخش تغییر دهید.

## ۴) درگاه پرداخت آنلاین (یونیک‌پی / UniquePay)

تنها درگاه پرداخت آنلاین پشتیبانی‌شده در این قالب **یونیک‌پی** است:

```
UNIQUEPAY_BASE_URL=https://uniquepay.top
UNIQUEPAY_BUSINESS_TOKEN=your-business-token
UNIQUEPAY_REDIRECT_URL=https://your-bot-domain.example.com
```

اگر `UNIQUEPAY_BUSINESS_TOKEN` خالی باشد، این روش پرداخت به‌طور خودکار از
لیست روش‌های پرداخت ربات مخفی می‌شود و کاربران فقط کارت‌به‌کارت/کیف پول
خواهند داشت.

## ۵) اطلاعات ربات (قالب فروشی)

به‌جای هاردکد کردن اطلاعات کسب‌وکار در کد، از بخش «ℹ️ اطلاعات ربات» در پنل
ادمین استفاده کنید تا مقادیر زیر را تنظیم/ویرایش کنید:

- متن خوش‌آمدگویی
- شماره کارت و نام صاحب حساب (برای پرداخت کارت‌به‌کارت)
- لینک پشتیبانی
- یوزرنیم ربات (برای لینک دعوت)
- لینک راهنمای اتصال
- آیدی عددی کانال لاگ سفارش‌ها
- لیست کانال‌های عضویت اجباری

همهٔ این مقادیر در دیتابیس ذخیره می‌شوند و از طریق پنل ادمین قابل تغییرند؛
نیازی به دست‌کاری کد نیست.

## ۶) حذف‌شده‌ها نسبت به نسخهٔ اختصاصی

- تمام بخش‌های مینی‌اپ (Mini App) و وب‌هوک مرتبط با آن حذف شده‌اند.
- ادغام با پنل VPN قبلی به‌طور کامل با مرزبان و پاسارگارد (هر دو به‌عنوان
  گزینه‌های قابل‌انتخاب پنل VPN) جایگزین شده است.
- Sentry به‌طور کامل حذف شده؛ خطاها فقط در دیتابیس (بخش «🦖 لاگ خطاها» در پنل
  ادمین) ذخیره و نمایش داده می‌شوند.
- تمام استیکرهای منو به‌صورت پیش‌فرض خالی هستند و باید از پنل ادمین آپلود شوند.
